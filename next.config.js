/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you want to rewrite asset paths or use assetResolver, you may need to
  // configure the static folder. Next serves everything in /public automatically.
};

module.exports = nextConfig;
